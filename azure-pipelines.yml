trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: 'test'
    arguments: '--configuration Release'

- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    publishWebProjects: false
    arguments: '-c Release --output $(Build.ArtifactStagingDirectory)/FluentCloudflare-publish'

- task: DotNetCoreCLI@2
  inputs:
    command: 'pack'
    arguments: '-c Release --output $(Build.ArtifactStagingDirectory)/FluentCloudflare-nupkg'

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Build.ArtifactStagingDirectory)'
  